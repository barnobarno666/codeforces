#include <bits/stdc++.h>
 
using namespace std;
int main(){
    int n;
    cin >> n;
    string x=to_string(n);
    int i=0;
    while (i < x.size()-2){
        if( (stoi(x[i])+ stoi(x[i+1]) +  stoi(x[i+2])) )

    }